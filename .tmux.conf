# 开启鼠标支持
set -g mouse on

# 解决 Neovim 配色变灰的问题（启用真彩色支持）
set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"
set -g status-position bottom   
set -g status-justify left
set -g status-style 'bg=#660034,fg=#88c0d0' 
set -g status-left '#[fg=#bf616a,bold] 🐱 #S '
set -g status-left-length 20
# 中间显示窗口列表
setw -g window-status-current-style 'fg=#ebcb8b,bold'
setw -g window-status-current-format ' #I:#W '
setw -g window-status-format ' #I:#W '

# 右侧显示时间
set -g status-right '#[fg=#ffffff] %Y-%m-%d %H:%M '
set -g prefix C-a

unbind C-b
bind C-a send-prefix

bind | split-window -h -c "#{pane_current_path}"
bind _ split-window -v -c "#{pane_current_path}"
# 无缝切换 nvim 和 tmux 分屏
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'

bind -r H resize-pane -L 5   # 向左扩 5 格
bind -r J resize-pane -D 5   # 向下扩 5 格
bind -r K resize-pane -U 5   # 向上扩 5 格
bind -r L resize-pane -R 5   # 向右扩 5 格
